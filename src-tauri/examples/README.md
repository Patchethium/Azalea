# Exmaples

It feels weird to run sub commands with examples, but this seems to be the common practice.

## Range

This is a sub command that collects optimal pitch range from every voice model and saves it to `ranges.toml`.

```bash
cargo run --example range
```
